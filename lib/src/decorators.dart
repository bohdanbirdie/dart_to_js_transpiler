class ExposeClass {
  final bool exposeAll;

  const ExposeClass({ this.exposeAll = false }) : assert(exposeAll != null);
}

class ExposeMethod {
  const ExposeMethod();
}

class Entrypoint {
  const Entrypoint();
}